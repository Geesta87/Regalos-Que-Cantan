{
  "name": "story-builder",
  "version": "1.0.0",
  "private": true,
  "description": "Animado story-video auto-build engine for Cloud Run (Kie scenes + hero motion + morph + FFmpeg render)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  }
}
