{
  "name": "rqc-backup-vault",
  "private": true,
  "description": "Nightly copy-only offsite backup of Supabase Storage + key DB tables to GCS (rqc-backup-vault). Contains no delete logic by design.",
  "main": "index.js",
  "scripts": { "start": "node index.js" },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@google-cloud/storage": "^7.12.0"
  }
}
