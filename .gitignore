dist
node_modules
.vercel

# Misc
.DS_Store
.env
.env.local

# Web-push VAPID key pair (private half - never commit; public half is in src/services/push.js)
.vapid-keys.local.txt

# In-house FFmpeg video-renderer POC: large test renders + downloaded order assets (never commit)
video-poc/

# Golden-exam fixtures: Whisper transcripts of CUSTOMER takes — this repo is
# public, so they must never be committed. See tests/golden/run-golden.cjs.
tests/golden/fixtures/
supabase/.temp/
deno.lock
